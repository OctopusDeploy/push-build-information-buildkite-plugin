services:
  lint:
    image: buildkite/plugin-linter:v3.0.0
    command: ["--id", "OctopusDeploy/push-build-information"]
    volumes:
      - ".:/plugin:ro"
  tests:
    image: buildkite/plugin-tester:v4.0.0
    volumes:
      - ".:/plugin:ro"
